# artifact-shield
In this project, the Java Backend acts as a "Data Firewall." It sits between a user's prompt and the LLM. It uses deterministic algorithms (Regex, Luhn's Algorithm, and Entropy checks) to scrub sensitive data.
