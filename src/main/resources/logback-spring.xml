<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <springProperty scope="context" name="splunkUrl" source="shield.logging.splunk.url" defaultValue=""/>
    <springProperty scope="context" name="splunkToken" source="shield.logging.splunk.token" defaultValue=""/>
    <springProperty scope="context" name="splunkEnabled" source="shield.logging.splunk.enabled" defaultValue="false"/>

    <!-- Console Appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%X{correlationId}] [%thread] %cyan(%logger{36}) : %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>

    <!-- Splunk HEC Appender (Conditional based on properties) -->
    <springProfile name="splunk">
        <if condition='property("splunkEnabled").contains("true")'>
            <then>
                <appender name="SPLUNK" class="com.splunk.logging.HttpEventCollectorLogbackAppender">
                    <url>${splunkUrl}</url>
                    <token>${splunkToken}</token>
                    <source>artifact-shield</source>
                    <sourcetype>_json</sourcetype>
                    <messageFormat>json</messageFormat>
                    <disableCertificateValidation>true</disableCertificateValidation>
                    <layout class="ch.qos.logback.classic.PatternLayout">
                        <pattern>{"timestamp":"%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}","level":"%level","correlationId":"%X{correlationId}","thread":"%thread","logger":"%logger","message":"%msg"}%n</pattern>
                    </layout>
                </appender>

                <root level="INFO">
                    <appender-ref ref="SPLUNK" />
                </root>
            </then>
        </if>
    </springProfile>

    <!-- Set Artifact Shield logic to log appropriately -->
    <logger name="io.dhoondlay.shield" level="INFO"/>
</configuration>
